name: flutter_ui_kit_theme
description: "Production-ready Material 3 design system for multi-app Flutter ecosystems. Supports light/dark mode, multi-brand overrides, and a full design token system."
version: 0.1.8
homepage:

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0
  flutter_ui_kit_widget:
    git:
      url: https://github.com/jun-young1993/flutter_ui_kit.git
      path: packages/flutter_ui_kit_widget

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
