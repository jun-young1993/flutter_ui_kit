import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_ui_kit_widget/flutter_ui_kit_widget.dart';

void main() {
  test('DsIconToggle is exported', () {
    // DsIconToggle이 barrel에서 정상적으로 export되는지 타입 확인
    expect(DsIconToggle, isNotNull);
  });
}
